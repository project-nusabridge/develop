export interface ContactInquiryType {
  id: string;
  title: string;
  description: string;
}

export interface ContactChannel {
  id: string;
  label: string;
  value: string;
  description: string;
}
